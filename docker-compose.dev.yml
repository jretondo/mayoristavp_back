version: '3.8'

services:
  mayoristavp-app-dev:
    image: node-point-sell-dev:latest
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
      - ./public:/app/public
    ports:
      - '3016:3016'
    env_file:
      - .env
